[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--tb=short -q"

[tool.coverage.run]
source = ["routers", "services", "models"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
fail_under = 80
